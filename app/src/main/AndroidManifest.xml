<?xml version="1.0" encoding="utf-8"?>
<!--
  Educational self-introspection overlay demo.

  Permissions, and why each one is here:
    SYSTEM_ALERT_WINDOW          - lets the service draw the floating button and
                                   the ImGui panel above other apps.
    FOREGROUND_SERVICE           - required to keep the overlay alive in a
                                   foreground service.
    FOREGROUND_SERVICE_SPECIAL_USE - API 34 requires every foreground service
                                   to declare a type; "specialUse" is the
                                   correct umbrella for a developer-tools
                                   overlay that fits no media/camera/etc box.
    POST_NOTIFICATIONS           - the foreground service posts a notification;
                                   on Android 13+ that needs this permission.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="false"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Material.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".DebugService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Educational developer-tools overlay (self-introspection demo)" />
        </service>
    </application>

</manifest>
